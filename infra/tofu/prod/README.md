# prod environment

OpenTofu root for prod-only resources (the `clint` Worker, `clint-materials`
bucket, prod DNS records, prod Supabase). State lives in the Scalr workspace
`clint-prod`. Candidate for VCS-driven remote execution with an approval gate.

To be populated in WS3 Phase C. See `../README.md` and
`docs/superpowers/specs/2026-06-10-ws3-iac-foundation-design.md`.
