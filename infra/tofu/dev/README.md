# dev environment

OpenTofu root for dev-only resources (the `clint-dev` Worker, `clint-materials-dev`
bucket, dev DNS records, dev Supabase). State lives in the Scalr workspace
`clint-dev`.

To be populated in WS3 Phase C. See `../README.md` and
`docs/superpowers/specs/2026-06-10-ws3-iac-foundation-design.md`.
